/* This file was automatically generated by TightDB. */
package com.tightdb.test;

import com.tightdb.*;
import com.tightdb.lib.*;

/**
 * This class represents a TightDB table and was automatically generated.
 */
public class TestPhoneTable extends AbstractSubtable<TestPhoneRow, TestPhoneView, TestPhoneQuery> {
	public static final EntityTypes<TestPhoneTable, TestPhoneView, TestPhoneRow, TestPhoneQuery> TYPES = new EntityTypes<TestPhoneTable, TestPhoneView, TestPhoneRow, TestPhoneQuery>(TestPhoneTable.class, TestPhoneView.class, TestPhoneRow.class, TestPhoneQuery.class);

	public final StringRowsetColumn<TestPhoneRow, TestPhoneView, TestPhoneQuery> type = new StringRowsetColumn<TestPhoneRow, TestPhoneView, TestPhoneQuery>(TYPES, table, 0, "type");
	public final StringRowsetColumn<TestPhoneRow, TestPhoneView, TestPhoneQuery> number = new StringRowsetColumn<TestPhoneRow, TestPhoneView, TestPhoneQuery>(TYPES, table, 1, "number");
	public TestPhoneTable(TableBase subtableBase) {
		super(TYPES, subtableBase);
	}
	public static void specifyStructure(TableSpec spec) {
        addStringColumn(spec, "type");
        addStringColumn(spec, "number");
    }

    public TestPhoneRow add(java.lang.String type, java.lang.String number) {
        try {
            long position = size();

			insertString(0, position, type);
			insertString(1, position, number);
		   insertDone();

            return cursor(position);
        } catch (Exception e) {
            throw addRowException(e);
        }
    }

    public TestPhoneRow insert(long position, java.lang.String type, java.lang.String number) {
        try {
        	insertString(0, position, type);
        	insertString(1, position, number);
           insertDone();

            return cursor(position);
        } catch (Exception e) {
            throw insertRowException(e);
        }
    }

}
