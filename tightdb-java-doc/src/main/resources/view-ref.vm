                <h2>View Class</h2>
                <p>
                The <code>findAll</code> method in the <code>Query</code> class returns a <code>View</code> object.
				You can interact with the View just as a regular table, and all changes to it will propagate to the original <a href="#table">table</a>.
                </p>

                <h3>View column fields</h3>
				
${columns}
				
                <table border="1" width="100%" cellpadding="3" cellspacing="0" summary="">
                <tbody><tr bgcolor="#CCCCFF" class="TableHeadingColor">
                <th align="left" colspan="2"><font size="+2">
                <b>Method Summary</b></font></th>
                </tr>
                
${method_overview}

				</tbody></table>
