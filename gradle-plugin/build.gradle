apply plugin: 'groovy'
apply plugin: 'maven'

repositories {
    jcenter()
}

dependencies {
    compile gradleApi()
    compile localGroovy()
    compile 'com.neenbedankt.gradle.plugins:android-apt:1.8'
}

sourceCompatibility = 1.6
targetCompatibility = 1.6

group = 'io.realm'
version = new File("${projectDir}/../version.txt").text.trim();

uploadArchives {
    repositories {
        mavenDeployer {
            repository(url: "file://localhost/Users/emanuelez/.android-sdk/extras/android/m2repository")
        }
    }
}
