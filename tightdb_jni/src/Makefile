lib_LIBRARIES = libtightdb-jni.a

libtightdb_jni_a_SOURCES       = $(wildcard *.cpp)
libtightdb_jni_a_LDFLAGS       = -ltightdb -lproc
libtightdb_jni_a_LDFLAGS_DEBUG = -ltightdb-dbg -lproc

include ../../generic.mk
